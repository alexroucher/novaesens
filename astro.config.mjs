import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Configuration standard pour assurer la compatibilité
  build: {
    // Configuration par défaut
    assets: '_astro'
  }
}); 