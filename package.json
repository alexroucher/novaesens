{
  "name": "novaesens",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "start": "astro preview",
    "deploy": "npm run build && firebase deploy",
    "deploy:hosting": "firebase deploy --only hosting"
  },
  "dependencies": {
    "astro": "^2.0.0"
  }
}